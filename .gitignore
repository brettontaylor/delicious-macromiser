node_modules/
.wrangler/
dist/

# Secrets — the path secret IS the credential for the authless v1 server.
.dev.vars
.env
.env.local

# D1 exports / local backups
backup.sql
backup-prod.sql
*.sqlite
*.sqlite3

# Generated recipe catalog (built from content/recipes at deploy time)
apps/server/src/generated/

*.log
.DS_Store

# Windows: `--file=/dev/null` creates a literal file named `nul` here.
nul

# Chat transcripts and anything that looks like personal health notes.
# This repo is PUBLIC — see GOTCHAS.
Claude-Chat*
*transcript*.txt
.gitnexus

# Regenerated by `npx gitnexus analyze` on every commit — machine-specific,
# and any hand-edit is overwritten. GitNexus guidance lives in CLAUDE.md.
AGENTS.md
